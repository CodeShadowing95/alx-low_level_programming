#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: command-line argument count
 * @argv: vector of command-lines arguments
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	printf("\n");

	return (0);
}
