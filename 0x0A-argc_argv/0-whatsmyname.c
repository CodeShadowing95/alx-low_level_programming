#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: command-line argument count
 * @argv: vector of command-lines arguments
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
