#include <stdio.h>
#include "main.h"

/**
 * set_string - Set the value of a pointer to a char
 *
 * @s: array of array or pointer to the array
 * @to: string
 * Return: nothing
 */
void set_string(char **s, char *to)
{
*s = to;
}
